name: Github Publish

on:
  release:
    types: [ published ]
  workflow_dispatch:

env:
  IMAGE_NAME: whist-server

jobs:
  push:
    strategy:
      matrix:
        arch: [ amd64, arm64 ]
      fail-fast: false

    runs-on: ubuntu-latest

    permissions:
      packages: write
      contents: read

    steps:
      - uses: actions/checkout@v3

      - name: Get Version
        id: get_version
        uses: battila7/get-version-action@v2

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v2

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push
        id: docker_build
        uses: docker/build-push-action@v3
        with:
          context: .
          push: true
          platforms: linux/${{ matrix.arch }}
          tags: |
            whist-team/whist-server:${{ matrix.arch }}-latest
            whist-team/whist-server:${{ matrix.arch }}-${{ steps.get_version.outputs.version-without-v }}

      - name: Image digest
        run: echo ${{ steps.docker_build.outputs.digest }}
